# Poetry App

## Setup
1. Clone repository.
2. Put all image assets in `/assets/`.
3. Serve via GitHub Pages (or any static host).

## Pages
- `index.html`: Login screen (password = today’s date).
- `app.html`: Infinite image gallery.

## Editing
- Add poem objects to `poems.json`.
- Tweak CSS in `/css/`.
